% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare_query.R
\name{prepare_query}
\alias{prepare_query}
\alias{print.px_query}
\title{Prepare a data query with smart defaults}
\usage{
prepare_query(
  api,
  table_id,
  ...,
  .codelist = NULL,
  max_default_values = 22,
  maximize_selection = FALSE,
  verbose = FALSE
)

\method{print}{px_query}(x, ...)
}
\arguments{
\item{api}{A \verb{<px_api>} object.}

\item{table_id}{A single table ID (character).}

\item{...}{Ignored.}

\item{.codelist}{Named list of codelist overrides.}

\item{max_default_values}{Maximum number of values for a variable to receive
a wildcard default. Defaults to \code{22} (covers e.g. Swedish län).}

\item{maximize_selection}{If \code{TRUE}, expand unspecified variables to include
as many values as possible while staying under the API cell limit.}

\item{verbose}{Print request details.}

\item{x}{A \verb{<px_query>} object.}
}
\value{
A \verb{<px_query>} object. Pass to \code{\link[=get_data]{get_data()}} via the \code{query}
parameter.
}
\description{
Bridges the gap between table/variable exploration and data fetching.
Fetches variable metadata, applies sensible defaults for variable
selections, and returns a query object that can be passed to \code{\link[=get_data]{get_data()}}.
}
\details{
Default selection strategy:
\itemize{
\item \strong{ContentsCode}: all values (\code{"*"})
\item \strong{Time variable}: most recent 10 periods (\code{px_top(10)})
\item \strong{Eliminable variables}: omitted (API aggregates automatically)
\item \strong{Small mandatory variables} (<= \code{max_default_values} values): all (\code{"*"})
\item \strong{Large mandatory variables}: first value only (\code{px_top(1)})
}

When \code{maximize_selection = TRUE}, the function expands selections to use
as much of the API's cell limit as possible. Expansion order: smallest
eliminable variables first, then smallest mandatory, then time last.

The returned query object prints a human-readable summary showing what
was selected for each variable and why. Modify selections before passing
to \code{get_data()} by assigning to the \verb{$selections} list.
}
\examples{
\donttest{
scb <- px_api("scb", lang = "en")
if (px_available(scb)) {

  # Prepare with defaults
  q <- prepare_query(scb, "TAB638")
  q

  # Override specific variables, let defaults handle the rest
  q <- prepare_query(scb, "TAB638", Region = c("0180", "1480"))

  # Maximize data within API limits
  q <- prepare_query(scb, "TAB638", maximize_selection = TRUE)

  # Fetch data from a prepared query
  get_data(scb, query = q)
}}
}
