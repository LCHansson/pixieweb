% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\name{get_data}
\alias{get_data}
\title{Fetch data from a PX-Web table}
\usage{
get_data(
  api,
  table_id,
  ...,
  query = NULL,
  .codelist = NULL,
  .output = "long",
  .comments = FALSE,
  simplify = TRUE,
  auto_chunk = TRUE,
  max_results = NULL,
  verbose = FALSE
)
}
\arguments{
\item{api}{A \verb{<px_api>} object.}

\item{table_id}{A single table ID (character). Vectors are not supported;
use \code{purrr::map()} + \code{dplyr::bind_rows()} for multiple tables.
Ignored when \code{query} is provided.}

\item{...}{Variable selections as named arguments. Each name is a variable
code, each value is one of:
\itemize{
\item A character vector of specific value codes (item selection)
\item \code{"*"} for all values
\item A \link{px_selections} helper: \code{px_all()}, \code{px_top()}, \code{px_bottom()},
\code{px_from()}, \code{px_to()}, \code{px_range()}
\item Omitted variables are eliminated if the API allows it
Ignored when \code{query} is provided.
}}

\item{query}{A \verb{<px_query>} object from \code{\link[=prepare_query]{prepare_query()}}. When provided,
\code{table_id}, \code{...}, and \code{.codelist} are taken from the query object.}

\item{.codelist}{Named list of codelist overrides
(e.g. \code{list(Region = "agg_RegionLan")}).}

\item{.output}{\code{"long"} (default, tidy) or \code{"wide"} (pivot content variables).}

\item{.comments}{Include footnotes/comments as an attribute.}

\item{simplify}{Add human-readable text label columns alongside codes.}

\item{auto_chunk}{Automatically split large queries that exceed the cell
limit into multiple requests. When \code{TRUE} (default), the variable
with the most values is split into batches, each request staying under
the limit. A progress bar is shown. Set to \code{FALSE} to error instead.}

\item{max_results}{Override the API's cell limit. When set, this value is
used instead of the limit reported by the API's config endpoint. Useful
for keeping result size manageable or for testing chunking behavior.}

\item{verbose}{Print request details.}
}
\value{
A tibble of data. See Details for column structure.
}
\description{
The core function for downloading statistical data. Variable selections
are passed as named arguments via \code{...}, or via a prepared query object
from \code{\link[=prepare_query]{prepare_query()}}.
}
\details{
When \code{simplify = TRUE} and \code{.output = "long"} (defaults), columns are:
\itemize{
\item \code{table_id}: back-reference to the source table
\item One pair per dimension: \code{{code}} (raw code) + \verb{\{code\}_text} (label)
\item \code{value}: the numeric measurement
}

When \code{simplify = FALSE}, only raw codes and \code{value} are returned.

When \code{.output = "wide"}, content variables are pivoted into separate columns.

When \code{auto_chunk = TRUE} and the query would exceed the API's cell limit,
\code{get_data()} automatically splits the request. It picks the variable with
the most values and batches its values so each request fits under the limit.
Requests are paced to respect the API's rate limit.
}
\examples{
\dontrun{
scb <- px_api("scb", lang = "en")

# Fetch with explicit selections
get_data(scb, "TAB638",
  Region = c("0180", "1480"),
  Tid = px_top(5)
)

# Fetch from a prepared query
q <- prepare_query(scb, "TAB638", Region = c("0180"))
get_data(scb, query = q)
}
}
