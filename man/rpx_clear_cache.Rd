% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache.R
\name{rpx_clear_cache}
\alias{rpx_clear_cache}
\title{Clear rpx cache files}
\usage{
rpx_clear_cache(entity = NULL, api = NULL, cache_location = rpx_cache_dir())
}
\arguments{
\item{entity}{Character entity to clear (e.g. \code{"tables"}, \code{"enriched"}),
or \code{NULL} (default) to clear all rpx cache files.}

\item{api}{A \verb{<px_api>} object. If provided, only cache files for that
API's alias are cleared. \code{NULL} (default) clears all APIs.}

\item{cache_location}{Directory to clear. Defaults to \code{\link[=rpx_cache_dir]{rpx_cache_dir()}}.}
}
\value{
\code{invisible(NULL)}
}
\description{
Removes cached API responses stored in the default or specified location.
Can selectively clear by entity type and/or API.
}
\examples{
\donttest{
scb <- px_api("scb")
if (px_available(scb)) {
  rpx_clear_cache()
  rpx_clear_cache(entity = "tables")
  rpx_clear_cache(api = scb)
  rpx_clear_cache(entity = "enriched", api = scb)
}}
}
