% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tables.R
\name{get_tables}
\alias{get_tables}
\title{Get tables from a PX-Web API}
\usage{
get_tables(
  api,
  query = NULL,
  id = NULL,
  updated_since = NULL,
  max_results = NULL,
  .timeout = 15,
  cache = FALSE,
  cache_location = pixieweb_cache_dir,
  verbose = FALSE
)
}
\arguments{
\item{api}{A \verb{<px_api>} object.}

\item{query}{Free-text search string (sent to API as server-side search).
On v2 APIs (e.g. SCB) the server-side search is an \strong{exact token match}
by default — \code{query = "befolk"} will not find tables with "befolkning"
in the title. Use explicit wildcards (\code{query = "befolk*"}) for prefix
matching. Wildcards can also appear mid-term (e.g. \verb{*arbets*}). This is
a property of the PX-Web server, not pixieweb; behaviour may vary by
agency. Case-insensitive. Non-ASCII characters (å/ä/ö etc.) are
URL-encoded automatically.}

\item{id}{Character vector of specific table IDs to retrieve.}

\item{updated_since}{Only return tables updated in the last N days (integer).}

\item{max_results}{Maximum number of tables to return.}

\item{.timeout}{Maximum seconds to spend on v1 hierarchy tree walks (default
15). Only relevant when a v1 API lacks a \verb{?query=} search endpoint and must
fall back to walking the folder tree. Increase for exhaustive searches on
large APIs. Has no effect on v2 APIs (which have native search).}

\item{cache}{Logical, cache results locally.}

\item{cache_location}{Cache directory. Defaults to \code{\link[=pixieweb_cache_dir]{pixieweb_cache_dir()}}.}

\item{verbose}{Print request details.}
}
\value{
A tibble with columns: \code{id}, \code{title}, \code{description}, \code{category},
\code{updated}, \code{first_period}, \code{last_period}, \code{time_unit}, \code{variables},
\code{subject_code}, \code{subject_path}, \code{source}, \code{discontinued}.
}
\description{
Search for and list statistical tables available on a PX-Web instance.
}
\examples{
\donttest{
scb <- px_api("scb", lang = "en")
if (px_available(scb)) {

  # Server-side search
  get_tables(scb, query = "population")

  # Fetch specific tables by ID
  get_tables(scb, id = c("TAB638", "TAB1278"))

  # Tables updated in the last 30 days
  get_tables(scb, updated_since = 30)
}}
}
