% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tables.R
\name{table_search}
\alias{table_search}
\title{Client-side search on a table tibble}
\usage{
table_search(table_df, query, column = NULL)
}
\arguments{
\item{table_df}{A tibble returned by \code{\link[=get_tables]{get_tables()}}.}

\item{query}{Character vector of search terms (combined with OR).}

\item{column}{Column names to search. \code{NULL} searches all character columns.}
}
\value{
A filtered tibble.
}
\description{
Filter an already-fetched table tibble by regex. Complements
\code{get_tables(query = ...)} which does server-side search. Use this for
further refinement on cached results.
}
\examples{
\donttest{
scb <- px_api("scb", lang = "en")
if (px_available(scb)) {
  tables <- get_tables(scb, query = "population")

  # Further filter by regex
  tables |> table_search("municipality")
}}
}
